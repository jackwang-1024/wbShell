1，通过shell，利用cli命令，把创建可用卷，创建发起方，创建vag的这些部署完集群必做的步骤整合到一个shell脚本，这样后面不需要每次部署完都手工执行了。这个shell脚本应该
放在客户端上执行。 OK 已用shell完成
2，手工部署环境的步骤，可以写到一个shell脚本中。（已完成 放在152上）
优化
2.1 增加日志文件
2.2 增加定时任务 ，定期删除早期的日志文件


3.空闲时候可以上网找些shell的面试题练练手
4，编写shell程序 ，定期删除过早的coredump文件（实用）OK 已用python完成

5，创建license的shell脚本（sp作为入参）